(define (f) (error "error f"))
(define (g) 'ok)
(let ((a (random 10))
      (b (random 10))
      (h f))
  (display a) (newline)
  (display b) (newline)
  (if (> a 5)
      (begin (set! h g)
             (if (< b 5)
                 (f)
                 (h)))
      'ok))