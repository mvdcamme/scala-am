(define (f) (error "error f"))
(define (ff) (error "error ff"))
(define (g) 'ok)
(let ((a (random 1000000))
      (b (random 3000000))
      (h f)
      (i f))
  (display a)
  (display b)
  (if (> a 5)
      (begin (set! h g)
             (set! i g)
             (if (> b 5)
                 (h)
                 (i)))
      'ok))