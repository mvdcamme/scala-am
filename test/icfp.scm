;(define (f) (error "error f"))
;(define (g) 'ok)
(let ((a (random 354454650))
      (b (random 300000000))
      (c 10))
      ;(h f)
      ;(i f))
  (display a) (newline)
  (display b) (newline)
  ;(set! a (random 1))
  (if (> a 5)
      (if (> b 5)
          'ok
          (error "i"))
      'ok))