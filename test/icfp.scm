;(define (f) (error "error f"))
;(define (g) 'ok)
(let ((a (random 45))
      (b (random 45))
      (c 10))
      ;(h f)
      ;(i f))
  (display a) (newline)
  (display b) (newline)
  ;(set! a (random 1))
  (if (> (+ a b) 50)
      'ok1
      'ok2)
  (if (> a 50)
      (if (> b 50)
          'ok
          (error "i"))
      'ok))