(define (f) (error "error f"))
(define (g) 'ok)
(let ((a (random 100000))
      (b (random 300000))
      (h f)
      (i f))
  (display a)
  (display b)
  (if (> a 5)
      (begin (set! h g)
             (set! i g)
             (if (> b 5)
                 (h)
                 (i)))
      'ok))